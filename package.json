{
  "name": "readmeforge",
  "version": "1.0.0",
  "description": "Generate beautiful README documentation from a config file",
  "main": "src/readmeforge.js",
  "bin": {
    "readmeforge": "src/readmeforge.js"
  },
  "scripts": {
    "start": "node src/readmeforge.js",
    "test": "node --test src/*.test.js 2>/dev/null || echo 'No tests — add tests in src/*.test.js'",
    "lint": "eslint src/ --ext .js",
    "tracker": "node src/achievement-tracker.js",
    "roadmap": "node src/achievement-tracker.js"
  },
  "keywords": ["readme", "documentation", "generator", "cli", "markdown"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "commander": "^12.0.0",
    "chalk": "^5.3.0",
    "js-yaml": "^4.1.0",
    "mustache": "^4.2.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
